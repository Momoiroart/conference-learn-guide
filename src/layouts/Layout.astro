<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Learn AI</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Outfit:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style is:global>
    :root {
      --bg-primary: #05030f;
      --bg-secondary: #0a0820;
      --bg-card: rgba(255,255,255,0.04);
      --text-primary: #f4f4f6;
      --text-secondary: #9898b8;
      --accent: #ff6b9d;
      --accent-secondary: #4af0e8;
      --violet: #a855f7;
      --border: rgba(255,255,255,0.07);
      --border-hover: rgba(255,107,157,0.4);
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      background: var(--bg-primary);
      color: var(--text-primary);
      font-family: 'Outfit', sans-serif;
      line-height: 1.6;
      overflow-x: hidden;
    }
    a { color: inherit; text-decoration: none; }
    .container { max-width: 1100px; margin: 0 auto; padding: 0 24px; }

    /* ── ANIMATED BACKGROUND ── */
    .bg-canvas {
      position: fixed;
      inset: 0;
      z-index: -2;
      background: radial-gradient(ellipse at 20% 20%, rgba(168,85,247,0.12) 0%, transparent 60%),
                  radial-gradient(ellipse at 80% 80%, rgba(74,240,232,0.08) 0%, transparent 60%),
                  radial-gradient(ellipse at 50% 50%, rgba(255,107,157,0.05) 0%, transparent 70%),
                  var(--bg-primary);
    }

    .orb {
      position: fixed;
      border-radius: 50%;
      pointer-events: none;
      filter: blur(90px);
      opacity: 0.35;
      animation: floatOrb 20s ease-in-out infinite;
      z-index: -1;
    }
    .orb-1 { width: 600px; height: 600px; background: #ff6b9d; top: -250px; left: -150px; animation-delay: 0s; }
    .orb-2 { width: 500px; height: 500px; background: #a855f7; top: 30%; right: -200px; animation-delay: -7s; }
    .orb-3 { width: 350px; height: 350px; background: #4af0e8; bottom: -100px; left: 25%; animation-delay: -14s; opacity: 0.2; }

    @keyframes floatOrb {
      0%, 100% { transform: translate(0, 0) scale(1); }
      33%       { transform: translate(40px, -40px) scale(1.08); }
      66%       { transform: translate(-25px, 25px) scale(0.94); }
    }

    .grid-overlay {
      position: fixed;
      inset: 0;
      background-image:
        linear-gradient(rgba(255,255,255,0.025) 1px, transparent 1px),
        linear-gradient(90deg, rgba(255,255,255,0.025) 1px, transparent 1px);
      background-size: 60px 60px;
      z-index: -1;
      pointer-events: none;
    }

    /* ── HEADER ── */
    header {
      position: fixed;
      top: 0; left: 0; right: 0;
      padding: 16px 0;
      background: rgba(5,3,15,0.75);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
      z-index: 100;
      border-bottom: 1px solid var(--border);
      transition: padding 0.3s;
    }
    .header-inner { display: flex; justify-content: space-between; align-items: center; }
    .logo { font-family: 'Space Grotesk', sans-serif; font-weight: 700; font-size: 20px; letter-spacing: -0.02em; }
    .logo span {
      background: linear-gradient(135deg, var(--accent), var(--accent-secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .nav-links { display: flex; gap: 32px; list-style: none; }
    .nav-links a {
      color: var(--text-secondary);
      font-size: 14px;
      font-weight: 500;
      transition: color 0.2s;
      position: relative;
    }
    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: -4px; left: 0;
      width: 0; height: 2px;
      background: linear-gradient(90deg, var(--accent), var(--accent-secondary));
      transition: width 0.25s ease;
      border-radius: 2px;
    }
    .nav-links a:hover { color: var(--text-primary); }
    .nav-links a:hover::after { width: 100%; }

    /* ── BUTTONS ── */
    .btn, a.btn, button.btn {
      display: inline-flex !important;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 13px 28px !important;
      border-radius: 12px !important;
      font-size: 15px !important;
      font-weight: 600 !important;
      font-family: 'Outfit', sans-serif !important;
      text-decoration: none !important;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1) !important;
      cursor: pointer;
      border: none !important;
      position: relative;
      overflow: hidden;
    }
    .btn-primary, a.btn-primary {
      background: linear-gradient(135deg, #ff6b9d, #c84be8) !important;
      color: #fff !important;
      box-shadow: 0 4px 20px rgba(255,107,157,0.3), 0 0 0 1px rgba(255,107,157,0.1);
    }
    .btn-primary:hover, a.btn-primary:hover {
      transform: translateY(-2px) !important;
      box-shadow: 0 8px 30px rgba(255,107,157,0.45), 0 0 0 1px rgba(255,107,157,0.2) !important;
    }
    .btn-secondary, a.btn-secondary {
      background: rgba(255,255,255,0.06) !important;
      border: 1px solid rgba(255,255,255,0.15) !important;
      color: var(--text-primary) !important;
      backdrop-filter: blur(8px);
    }
    .btn-secondary:hover, a.btn-secondary:hover {
      border-color: var(--accent-secondary) !important;
      background: rgba(74,240,232,0.1) !important;
      color: var(--accent-secondary) !important;
      transform: translateY(-2px) !important;
    }

    /* ── CARDS ── */
    .lesson-card {
      background: var(--bg-card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 32px;
      margin-bottom: 24px;
      backdrop-filter: blur(12px);
      transition: border-color 0.2s, transform 0.2s;
    }
    .lesson-card:hover { border-color: rgba(168,85,247,0.3); transform: translateY(-2px); }
    .lesson-card h2 { font-family: 'Space Grotesk', sans-serif; font-size: 22px; margin-bottom: 16px; }
    .lesson-card p { color: var(--text-secondary); line-height: 1.8; }

    /* ── STEPS ── */
    .steps { margin-top: 20px; }
    .step { display: flex; gap: 16px; padding: 16px 0; border-bottom: 1px solid var(--border); }
    .step:last-child { border-bottom: none; }
    .step-num {
      width: 32px; height: 32px;
      background: linear-gradient(135deg, var(--accent), var(--violet));
      color: #fff;
      border-radius: 50%;
      display: flex; align-items: center; justify-content: center;
      font-weight: 600; flex-shrink: 0; font-size: 14px;
    }
    .step h4 { margin-bottom: 4px; }
    .step p { font-size: 14px; color: var(--text-secondary); }

    /* ── CODE ── */
    .code-block {
      background: rgba(0,0,0,0.4);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 20px;
      margin: 16px 0;
      overflow-x: auto;
    }
    .code-block code { font-family: 'Fira Code', monospace; color: var(--accent-secondary); font-size: 14px; }

    /* ── NAVIGATION ARROWS ── */
    .navigation { display: flex; justify-content: space-between; gap: 16px; margin-top: 40px; }

    /* ── HERO ── */
    .hero { padding: 120px 0 60px; }
    .hero-badge {
      display: inline-block;
      padding: 6px 16px;
      background: rgba(168,85,247,0.12);
      border: 1px solid rgba(168,85,247,0.3);
      border-radius: 100px;
      color: #c084fc;
      font-size: 12px;
      font-weight: 500;
      letter-spacing: 0.05em;
      margin-bottom: 20px;
    }
    .hero h1 { font-family: 'Space Grotesk', sans-serif; font-size: clamp(32px, 6vw, 56px); font-weight: 700; margin-bottom: 16px; }
    .gradient {
      background: linear-gradient(135deg, var(--accent), var(--violet), var(--accent-secondary));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .lead { font-size: 18px; color: var(--text-secondary); max-width: 600px; }

    /* ── CONTENT SECTION ── */
    .content { padding: 40px 0 80px; }

    /* ── FOOTER ── */
    footer {
      padding: 40px 0;
      text-align: center;
      border-top: 1px solid var(--border);
      margin-top: 80px;
    }
    footer p { color: var(--text-secondary); font-size: 14px; }
  </style>
</head>
<body>
  <!-- Animated background -->
  <div class="bg-canvas"></div>
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>
  <div class="orb orb-3"></div>
  <div class="grid-overlay"></div>

  <header>
    <div class="container header-inner">
      <a href="/" class="logo">Learn<span>AI</span></a>
      <nav>
        <ul class="nav-links">
          <li><a href="/">Home</a></li>
          <li><a href="/kids">Kids Track</a></li>
          <li><a href="/adults">Adults Track</a></li>
          <li><a href="/templates">Templates</a></li>
          <li><a href="/ai-setup">AI Setup</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main style="padding-top: 80px;">
    <slot />
  </main>

  <footer>
    <div class="container">
      <p>© 2026 Learn AI. Built with <span style="color: var(--accent);">❤️</span> and AI.</p>
    </div>
  </footer>

  <script>
    // Shrink header on scroll
    const header = document.querySelector('header');
    window.addEventListener('scroll', () => {
      if (window.scrollY > 50) {
        header.style.padding = '10px 0';
        header.style.background = 'rgba(5,3,15,0.95)';
      } else {
        header.style.padding = '16px 0';
        header.style.background = 'rgba(5,3,15,0.75)';
      }
    });
  </script>
</body>
</html>
