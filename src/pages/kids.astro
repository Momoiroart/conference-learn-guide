---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Kids Track - Learn AI">
  <section class="hero">
    <div class="container">
      <span class="hero-badge">🎯 Kids Track</span>
      <h1>Learn AI the <span class="gradient">Fun Way!</span></h1>
      <p class="lead">A step-by-step journey to build your own websites, games, and portfolios. No experience needed!</p>
      <div class="hero-cta">
        <a href="/level-1" class="btn btn-primary">Start Learning →</a>
      </div>
    </div>
  </section>

  <section>
    <div class="container">
      <span class="section-label">Your Journey</span>
      <h2 class="section-title">4 Levels to Complete</h2>
      
      <div class="levels-grid">
        <a href="/level-1" class="level-card">
          <span class="level-num">1</span>
          <div class="level-content">
            <h3>What is AI?</h3>
            <p>Learn what Artificial Intelligence is and how it works.</p>
          </div>
        </a>
        
        <a href="/level-2" class="level-card">
          <span class="level-num">2</span>
          <div class="level-content">
            <h3>Meet Ollama</h3>
            <p>Install and use Ollama - your very own AI!</p>
          </div>
        </a>
        
        <a href="/level-3" class="level-card">
          <span class="level-num">3</span>
          <div class="level-content">
            <h3>Your First Project</h3>
            <p>Build your very first website!</p>
          </div>
        </a>
        
        <a href="/level-4" class="level-card">
          <span class="level-num">4</span>
          <div class="level-content">
            <h3>Portfolio Tips</h3>
            <p>Make your portfolio stand out!</p>
          </div>
        </a>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero { padding: 100px 0 60px; text-align: center; }
  .hero-badge { display: inline-block; padding: 8px 16px; background: rgba(255,107,157,0.1); border: 1px solid rgba(255,107,157,0.3); border-radius: 100px; color: var(--accent); font-size: 12px; margin-bottom: 20px; }
  .hero h1 { font-family: 'Space Grotesk', sans-serif; font-size: clamp(36px, 6vw, 60px); font-weight: 700; margin-bottom: 16px; }
  .gradient { background: linear-gradient(135deg, var(--accent), var(--accent-secondary)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .lead { font-size: 18px; color: var(--text-secondary); max-width: 600px; margin: 0 auto 32px; }
  .hero-cta { display: flex; gap: 16px; justify-content: center; }

  section { padding: 60px 0; }
  .section-label { font-size: 11px; letter-spacing: 0.2em; text-transform: uppercase; color: var(--accent-secondary); margin-bottom: 12px; display: block; text-align: center; }
  .section-title { font-family: 'Space Grotesk', sans-serif; font-size: clamp(28px, 4vw, 40px); font-weight: 700; margin-bottom: 40px; text-align: center; }

  .levels-grid { display: grid; gap: 16px; }
  .level-card { display: flex; align-items: center; gap: 20px; background: var(--bg-card); border: 1px solid var(--border); border-radius: 16px; padding: 24px; text-decoration: none; color: inherit; transition: all 0.2s; }
  .level-card:hover { border-color: var(--accent); transform: translateX(8px); }
  .level-num { width: 48px; height: 48px; background: linear-gradient(135deg, var(--accent), var(--accent-secondary)); border-radius: 12px; display: flex; align-items: center; justify-content: center; font-family: 'Space Grotesk', sans-serif; font-weight: 700; font-size: 18px; flex-shrink: 0; }
  .level-content h3 { font-family: 'Space Grotesk', sans-serif; font-size: 16px; margin-bottom: 4px; }
  .level-content p { color: var(--text-secondary); font-size: 14px; }
</style>
